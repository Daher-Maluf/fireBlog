import { TransformarfechaPipe } from './transformarfecha.pipe';

describe('TransformarfechaPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformarfechaPipe();
    expect(pipe).toBeTruthy();
  });
});
